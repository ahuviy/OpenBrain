I created the two migration files as specified, following the existing convention from migrations 001 and 002 of wrapping everything in BEGIN/COMMIT. The up migration adds two generated columns that extract contentHash and codeHash from the JSONB provenance sub-object using the arrow operators, creates partial indexes that skip NULL values for both, and defines a new match_thoughts_by_source RPC function. The down migration drops these in reverse-dependency order with full IF EXISTS guards.

No dead-ends were encountered since this was purely additive file creation with a well-defined spec. The key gotcha for future slices is that the JSONB keys are camelCase (contentHash, codeHash) matching Hallmark v1, not snake_case — if upstream ever changes those keys, the generated columns will silently return NULL. Another consideration is that ADD COLUMN GENERATED STORED can cause a full table rewrite on PostgreSQL 16 and below, so operators should schedule this during low-traffic windows.

Future slices that need to reference provenance data should use the source_file_hash and code_hash generated columns directly rather than re-extracting from JSONB. The match_thoughts_by_source function lives in the public schema and accepts a source_hash text parameter for lookups. If a future slice needs to add code_hash-based lookup, a similar RPC pattern would apply.